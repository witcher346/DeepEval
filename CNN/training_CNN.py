from keras.layers import Conv2D, MaxPool2D
from keras.preprocessing.image import ImageDataGenerator
from PIL import Image



print(data)
input()
df = pd.DataFrame(data, columns=['img', 'price'])
print(df)